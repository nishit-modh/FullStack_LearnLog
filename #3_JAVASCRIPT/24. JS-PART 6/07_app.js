//Actions that can be performed on an object.
//Math.random() is a example of method.

const cal = {
    sum : function( a, b){
        return a + b;
    },
    sub : function (a, b){
        return a - b;
    },
    mul(a, b){ //method without directly mrntioning function
        return a * b;
    },
    div(a, b){
        return a / b;
    }
}